import java.util.*

fun main() {
  // store the string in a variable I am a Kotlin programmer now 
  // store the value of the position of vowels in that string to other array
  var str: String = "I am a Kotlin programmer now"
  var arr: Array<Int> = arrayOf(0, 2, 5, 8, 11, 16, 19, 22, 26)
  println(Arrays.toString(arr))
  print(arr.sum())
}
